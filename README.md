# healthy-meal-detector
Upload a meal photo → perform high-res semantic color classification (300*300) → map to 30*30 block grid (plate/green/yellow/brown/red) → rearrange by class → compute green block ratio → display animated GIF + metrics.
